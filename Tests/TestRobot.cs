using System.Text;
using NUnit.Framework;
using RobotTask;

namespace Tests
{
    [TestFixture]
    public class TestRobot
    {/*
        [Test]
        public void TestTryReplaceOnceTrue()
        {
            var sb = new StringBuilder("Hello, World!");
            var rw = new StringBuilder("l");
            var ww = new StringBuilder("low");
            sb.ToString().TryReplaceOnce(rw, ww, var out result);

            Assert.IsTrue(result);
            Assert.AreEqual("Helowlo, World!",sb.ToString());
        }

        [Test]
        public void TestTryReplaceOnceFalse()
        {
            var sb = new StringBuilder("Hello, World!");
            var rw = new StringBuilder("low");
            var ww = new StringBuilder("l");
            var result = sb.TryReplaceOnce(rw, ww);

            Assert.IsFalse(result);
            Assert.AreEqual("Hello, World!", sb.ToString());
        }
        */
    }
}